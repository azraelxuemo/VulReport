Conditions for Execution：      
  Need a regular account 
 
 Vuln place:
  login,and use 系统监控->定时任务，where we can invoke java methods,in ruoyi-quartz/src/main/java/com/ruoyi/quartz/controller/SysJobController.java,addsave or editSave function,
   there you can see a lot limit
    And I find a class which will cause ssrf, but the project limit http://,or https://,so we can only use ftp:// to probe network.We can compare the time cause to judge whether the port open or not.If a long time proves that the port is closed
 
 #Poc
  so we add a new cronjob,and the invoke string is com.ruoyi.common.utils.http.HttpUtils.sendGet('ftp://127.0.0.1:10001')
    And then run this cronjob ,listen the target port,you will recive the connection from victim
   
 #patch
  disable com.rouyi.common.utils.http class
  
